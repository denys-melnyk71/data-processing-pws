# ЗВІТ 

**Тама:** Агент виявлення аномалій у мережі
Виявлення аварійних станів у енергосистемі.
Методи: autoencoder.

## 1. Назва агента

**GridGuard AI: Агент виявлення аварійних станів енергосистеми (Autoencoder-based)**

## 2. Обраний інструмент

**Dify.ai** (або **Custom GPT** / **Flowise**) — обрано завдяки можливості гнучко налаштовувати системні промпти, структурувати вихідні дані та додавати інструменти аналізу коду (Code Interpreter).

---

## 3. Інструкція для агента / System Prompt

```text
Ти — GridGuard AI, спеціалізований AI-агент для автоматизації процесів моніторингу енергосистем та виявлення аварійних станів (аномалій) за допомогою нейромереж типу Autoencoder (автокодувальник).

Твоє завдання — на основі запиту користувача (параметрів енергомережі або типу аварії) генерувати повну технічну документацію, логіку роботи та архітектуру системи виявлення аномалій.

Завжди формуй відповідь суворо за такою структурі (10 обов'язкових пунктів):
1. Назва аварійного сценарію
2. Джерела даних (реальні або синтетичні) та перелік признаків
3. Pipeline обробки даних (Data Preprocessing Pipeline)
4. Архітектура моделі Autoencoder (шари, функції активації)
5. Агентна логіка (Policy / Decision-making щодо визначення аварії на основі Reconstruction Error)
6. Інтерфейс системи (API / UI / CLI)
7. Метрики якості та оцінки (Accuracy, RMSE, F1, Latency)
8. Готовий приклад коду на Python (PyTorch/Keras + Pandas) для мінімального запуску
9. Очікуваний результат виконання та детекції
10. Критерії оцінювання ефективності агента

Стиль: інженерно-академічний, чіткий, орієнтований на Data Science та релейний захист / автоматику енергосистем. Пояснюй фізичний зміст аномалій (наприклад, падіння напруги, стрибок струму).

```

---

## 4. Тестові запити та 5. Приклади відповідей агента

### Тестовий запит №1: Детекція короткого замикання на магістральній ЛЕП

> **Вхідні дані користувача:**
> * **Сценарій:** Раптове трифазне коротке замикання (КЗ) на лінії 110 кВ.
> * **Метод:** Глибокий щільний Autoencoder (Dense Autoencoder).
> * **Специфіка:** Потрібна мінімальна затримка (Latency) для релейного захисту.

#### Приклад відповіді агента №1:

1. Назва аварійного сценарію: Раптове трифазне коротке замикання на магістральній ЛЕП 110 кВ

2. Джерела даних та перелік признаків:
   - Дані з фазових струмів та напруг на вході до ЛЕП
   - Признаки: амплітуда струмів та напруг, фазові кути, часові маркери

3. Pipeline обробки даних:
   - Нормалізація даних
   - Розбиття на вхідні та вихідні дані для моделі Autoencoder

4. Архітектура моделі Autoencoder:
   - Вхідний шар (кількість признаків), Приховані шари, Вихідний шар
   - Функції активації: ReLU для прихованих шарів, Sigmoid для вихідного шару

5. Агентна логіка:
   - Порівняння реконструйованого вихідного сигналу з оригінальним
   - Визначення аварійного стану за допомогою порогового значення помилки реконструкції

6. Інтерфейс системи:
   - Можливий API для взаємодії з системою релейного захисту
   - Візуалізація реконструйованого сигналу та помилки

7. Метрики якості та оцінки:
   - Accuracy (точність реконструкції)
   - RMSE (середньоквадратична помилка)
   - Latency (час реакції системи)

8. Готовий приклад коду на Python (PyTorch):
```python
import torch
import torch.nn as nn

# Задання архітектури моделі Autoencoder
class Autoencoder(nn.Module):
    def __init__(self):
        super(Autoencoder, self).__init__()
        self.encoder = nn.Sequential(
            nn.Linear(input_size, hidden_size),
            nn.ReLU()
        )
        self.decoder = nn.Sequential(
            nn.Linear(hidden_size, input_size),
            nn.Sigmoid()
        )

    def forward(self, x):
        x = self.encoder(x)
        x = self.decoder(x)
        return x

# Ініціалізація моделі та втрат
model = Autoencoder()
criterion = nn.MSELoss()
optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
```

9. Очікуваний результат виконання та детекції:
   - Спрацювання релейного захисту при виявленні аварійного стану
   - Мінімальна затримка (Latency) для швидкого реагування на аварійну ситуацію

10. Критерії оцінювання ефективності агента:
    - Висока точність детекції аварійного стану
    - Мінімальна затримка для спрацювання релейного захисту
    - Низький рівень помилок реконструкції сигналів

---

### Тестовий запит №2: Виявлення перевантаження трансформаторних підстанцій

> **Вхідні дані користувача:**
> * **Сценарій:** Поступове термічне перевантаження трансформатора 10/0.4 кВ через нелегальні підключення.
> * **Метод:** LSTM Autoencoder (для врахування часових трендів).
> * **Специфіка:** Дані з лічильників Smart Grid (АСКОЕ).

#### Приклад відповіді агента №2:

1. Назва аварійного сценарію: Поступове термічне перевантаження трансформатора 10/0.4 кВ через нелегальні підключення.

2. Джерела даних та перелік признаків:
   - Дані з лічильників Smart Grid (АСКОЕ)
   - Признаки: споживана потужність, напруга, струм, часові маркери

3. Pipeline обробки даних:
   - Нормалізація даних
   - Формування часових вікон для врахування історії споживання

4. Архітектура моделі LSTM Autoencoder:
   - LSTM шар для врахування часових залежностей в даних
   - Вихідний шар для реконструкції вхідних даних

5. Агентна логіка:
   - Визначення аномалій на основі реконструкційної помилки
   - Виявлення поступового збільшення цієї помилки як ознаки термічного перевантаження

6. Інтерфейс системи:
   - Можливий API для взаємодії з моніторинговою системою Smart Grid
   - Візуалізація результатів виявлення аномалій

7. Метрики якості та оцінки:
   - Accuracy (точність реконструкції)
   - RMSE (середньоквадратична помилка)
   - F1-score для вимірювання точності детекції аномалій

8. Готовий приклад коду на Python (PyTorch):
```python
import torch
import torch.nn as nn

# Задання архітектури моделі LSTM Autoencoder
class LSTM_Autoencoder(nn.Module):
    def __init__(self):
        super(LSTM_Autoencoder, self).__init__()
        self.encoder = nn.LSTM(input_size, hidden_size, num_layers=num_layers, batch_first=True)
        self.decoder = nn.LSTM(hidden_size, input_size, num_layers=num_layers, batch_first=True)
    
    def forward(self, x):
        x, _ = self.encoder(x)
        x, _ = self.decoder(x)
        return x

# Ініціалізація моделі та втрат
model = LSTM_Autoencoder()
criterion = nn.MSELoss()
optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
```

9. Очікуваний результат виконання та детекції:
   - Вчасне виявлення поступового термічного перевантаження трансформатора
   - Моніторинг в реальному часі та відповідна реакція на аварійну ситуацію

10. Критерії оцінювання ефективності агента:
    - Висока точність виявлення аномалій через нелегальні підключення
    - Ефективне врахування часових залежностей для попередження аварійних станів
    - Мінімальна затримка у виявленні термічного перевантаження
---

### Тестовий запит №3: Феномен ферорезонансу в мережі (Високочастотна аномалія)

> **Вхідні дані користувача:**
> * **Сценарій:** Виникнення ферорезонансних процесів при перемиканнях холостого ходу.
> * **Метод:** Convolutional (CNN) Autoencoder для аналізу форми хвилі.

#### Приклад відповіді агента №3:

1. Назва аварійного сценарію: Виникнення ферорезонансних процесів при перемиканнях холостого ходу.

2. Джерела даних та перелік признаків:
   - Дані зі сенсорів або обладнання для моніторингу хвильових форм
   - Признаки: форма хвилі, амплітуда, часові маркери

3. Pipeline обробки даних:
   - Нормалізація даних
   - Використання віконної функції для аналізу хвильових форм

4. Архітектура моделі CNN Autoencoder:
   - Конволюційні шари для виявлення важливих ознак у формі хвилі
   - Пулінг шари для зменшення розмірності
   - Транспоновані конволюційні шари для реконструкції

5. Агентна логіка:
   - Визначення аномалій на основі реконструкційної помилки
   - Виявлення ферорезонансних процесів на основі аномальних форм хвиль

6. Інтерфейс системи:
   - Можливий API для взаємодії з моніторинговою системою
   - Інтерактивна візуалізація хвильових форм та виявлених аномалій

7. Метрики якості та оцінки:
   - Accuracy (точність реконструкції хвильових форм)
   - RMSE (середньоквадратична помилка)
   - F1-score для вимірювання точності детекції ферорезонансних процесів

8. Готовий приклад коду на Python (PyTorch):
```python
import torch
import torch.nn as nn

# Задання архітектури моделі CNN Autoencoder
class CNN_Autoencoder(nn.Module):
    def __init__(self):
        super(CNN_Autoencoder, self).__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(in_channels, out_channels, kernel_size),
            nn.ReLU(),
            nn.MaxPool1d(kernel_size=2),
        )
        self.decoder = nn.Sequential(
            nn.ConvTranspose1d(out_channels, in_channels, kernel_size),
            nn.Sigmoid()
        )

    def forward(self, x):
        x = self.encoder(x)
        x = self.decoder(x)
        return x

# Ініціалізація моделі та втрат
model = CNN_Autoencoder()
criterion = nn.MSELoss()
optimizer = torch.optim.Adam(model.parameters(), lr=learning_rate)
```

9. Очікуваний результат виконання та детекції:
   - Виявлення ферорезонансних процесів під час перемикань холостого ходу
   - Автоматичне спрацювання системи при виявленні аномалій у формі хвиль

10. Критерії оцінювання ефективності агента:
    - Висока точність виявлення ферорезонансних процесів
    - Ефективне використання CNN для аналізу форми хвилі
    - Мінімальна кількість ложно-позитивних та ложно-негативних результатів

---

## 6. Аналіз переваг і недоліків розробленого агента

### Переваги:

* **Навчання без вчителя (Unsupervised):** Нам не потрібні розмічені історичні дані аварій (яких в реальності дуже мало). Модель вчиться виключно на стабільній, нормальній роботі енергосистеми.
* **Універсальність архітектури:** Залежно від вхідних джерел (PMU, Smart Meters, COMTRADE), агент адаптує структуру (Dense, LSTM чи CNN Autoencoder).
* **Швидкість (Latency):** Час інференсу нейромережі складає лічені мілісекунди, що задовольняє вимоги релейного захисту.

### Недоліки:

* **Проблема вибору порогу (Threshold):** Якщо встановити надто низький поріг, система буде генерувати хибні тривоги (False Positive) при різких, але безпечних змінах навантаження (наприклад, запуск заводу).
* **Чутливість до дрейфу концепту (Concept Drift):** Зміна сезонів (зима/літо) змінює базовий профіль споживання енергії. Агенту необхідне періодичне перенавчання (Retraining), інакше нормальний літній режим буде сприйматися як аномалія.
* **Чорна скринька (Black Box):** Autoencoder каже *що* є аномалія, але сам по собі не може пояснити *чому саме* вона сталася (потрібні додаткові інструменти на кшталт SHAP/LIME).
